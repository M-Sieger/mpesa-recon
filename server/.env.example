# Database Configuration
# Use Postgres in production; for local dev without Postgres, comment the line below
DATABASE_URL=postgresql://postgres:dev@localhost:5432/mpesa_recon
# Optional SQLite fallback that kicks in when the primary DB is unreachable
SQLITE_FALLBACK_URL=sqlite:///./mpesa_recon.db

# API Configuration
SECRET_KEY=your-secret-key-change-in-production
API_V1_PREFIX=/api/v1
PROJECT_NAME=M-Recon API

# CORS Settings (comma-separated)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Environment
ENVIRONMENT=development

# File Upload Settings
MAX_UPLOAD_SIZE=10485760  # 10MB in bytes
ALLOWED_FILE_TYPES=pdf,csv

# Parsing Settings
MIN_PARSING_CONFIDENCE=0.95
